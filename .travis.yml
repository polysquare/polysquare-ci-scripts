language: c
sudo: false
cache:
  directories:
    - ~/.install
before_install:
 - source travis/setup-local-languages.sh ~/.install
script:
 - bash travis/shell-lint.sh -d travis
 - bash travis/project-lint.sh -d travis -e sh
 - bash travis/prepare-local-language-caching.sh ~/.install
